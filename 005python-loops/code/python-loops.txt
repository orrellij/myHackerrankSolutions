if __name__ == '__main__':
    n = int(input())

for x in range(0,n):   # telling it to put x to the power of two using the arithmetic operator **
    print(x**2)        # the range 0,n works because it excludes n from that list.  
